{
  "name": "line-assembly-system",
  "private": true,
  "version": "1.0.0",
  "description": "Root project: run the backend API and Electron frontend together with one command.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run electron:dev --prefix frontend",
    "dev": "concurrently -k -n BACKEND,FRONTEND -c green,cyan \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
